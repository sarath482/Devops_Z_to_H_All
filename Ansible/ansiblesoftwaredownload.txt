#Ansible download commands in linux

#Update linux packages before download

sudo apt Update

sudo apt install Ansible
or

sudo apt install ansible -y

# Verify Ansible Installation

ansible --version

#ansible [core 2.16.3]

################
# After ansible insatllion provide password less authentication for linux servers.

# Generate ssh-keys in linux servers.

ssh-keygen

# Then go to .ssh folder 

copy id_.pub (key) Then paste this publick key in target machines .ssh folder authorized_keys file.

# copy one system to another system use ssh/manual .

#check passwordless authentication working or not.

ssh serveripaddress